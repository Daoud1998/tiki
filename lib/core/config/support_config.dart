

class SupportConfig {
  SupportConfig._();

  /// WhatsApp number for contacting admin/support.
  /// Keep it fixed (no Remote Config) to avoid extra dependencies.
  static const String whatsapp = '+22236566606';
}
